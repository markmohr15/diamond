# Diamond server (Node/Express/TS + Postgres)

Milestone 2+. Thin by constitution (CLAUDE.md §Architecture): auth, team membership, append-only
event ingest, websocket fan-out. No stat computation here, ever. Schema types come from codegen —
never hand-written.
