# DIA-001 — Flutter scaffold + Drift event store
**Spec:** §2, §6, §7, §21
**Goal:** `flutter create` the app (org `app.diamond`), add Riverpod + Drift + very_good_analysis.
Implement the append-only event store: events table matching the envelope schema; append, readStream(gameId)
ordered by (wallClock, deviceId, seq); void + correction chains resolved at read time (§6).
**Accept:** unit tests: append/read ordering; voiding hides an event; a correction chain returns latest;
store survives app restart (integration test). No schema types hand-written — temporary hand types allowed
ONLY in this ticket, deleted in DIA-002.
**Out of scope:** UI, sync, snapshots (§7 checkpoint embedding lands with DIA-003).
