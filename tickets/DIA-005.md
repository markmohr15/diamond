# DIA-005 — Zone canvas widget
**Spec:** §3.1, §11.1, §18.7
**Goal:** CustomPaint strike-zone canvas: renders zone rect + out-of-zone margin; tap → ZoneCoord
(absolute x; handedness flip at render only); shows tap marker; supports underlay slot (future §18.6);
"skip location" affordance. Tablet + phone layouts.
**Accept:** widget tests: tap→coord math incl. out-of-zone, handedness rendering flip, golden tests
for both form factors. Design language §18.7 review pass.
