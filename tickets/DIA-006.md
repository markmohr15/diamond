# DIA-006 — Call screen + code display
**Spec:** §10.1–10.3
**Goal:** pitch-type row (team-config stub: 4 types) + call-zone grid (default 3×3+4 layout from §10.1);
tap-tap → pending call + HUGE code (≥120pt) from a stub WristbandCard (grid-coordinate model, k=2);
re-roll swipe; call replacement before commit; waste-pitch tap-and-hold.
**Accept:** widget tests: two taps produce pending call w/ correct centroid + a valid code for that call;
re-roll returns different code same call; nothing persists to the event store until pitch entry (DIA-007).
**Out of scope:** card generation/print (M2), decoys, offense grid.
