# DIA-004 — Rules engine: official scoring derivation + fixture suite green
**Spec:** §13, §14; fixtures/plays/
**Goal:** derive from the stream: putouts/assists, official errors per §13.2 (misplay + ordinaryEffort +
consequence), hit-vs-error, misplay ledger (development view), earned-run reconstruction (§13.3).
Build the fixture runner (wraps payloads in envelopes, folds, asserts `expect`).
**Accept:** all six fixtures pass. Fixture runner is a single command (`flutter test test/fixtures_test.dart`).
Resolve the two engine notes inside fixtures 05/06 (non-BallInPlay touch anchors): propose schema addition
(PickoffAttempt) to Mark before implementing.
