# DIA-003 — GameState projection engine
**Spec:** §5, §12.5, §7
**Goal:** pure fold: List<GameEvent> → GameState {count, outs, inning half, bases, score, batter due,
pitch counts}. Handle: outcome-driven count logic (foul at 2 strikes, foul_bunt K, D3K arming),
`unknown` outcome → uncertain-count flag, CountCorrection override + back-inference (mark inferred),
VoidEvent/correction chains, InningHalfStart snapshot checkpointing (write + fast-path read).
**Accept:** property test: fold(events) == fold(snapshot + tail). Unit tests for every count edge in §4.1.
Uncertainty flag set/cleared per §12.5.
**Out of scope:** official scoring derivation (DIA-004), stats.
