# DIA-007 — The pitch loop
**Spec:** §11.1–11.3, §12.5
**Split (agreed with Mark):** two PRs off this ticket. **007a** — core loop: session/store plumbing,
flow state machine, call → actual → outcome → commit, count HUD (amber uncertainty), top-level undo,
plain-K RunnerOut. **007b** — bailout swipe, CountCorrection sheet, walk forced-advance confirm,
D3K arming prompt, "record last pitch" (§11.1 v0.39), batter-action chips if in scope, harness cleanup.
**Goal:** wire call screen → zone canvas (actual) → outcome confirm (suggested from location, override row)
→ emits PitchThrown with intended+actual; loop returns to call screen; count HUD from DIA-003 projection
(amber uncertainty state); bailout row (two-finger swipe); long-press count → CountCorrection sheet;
top-level undo (VoidEvent).
**Accept:** integration test: scripted 6-pitch AB (incl. one unknown + correction) produces the exact
expected event stream and HUD states. Walk auto-generates forced advances w/ confirm (§11.3). One
in-play pitch is located *after* the play via "record last pitch," asserting the §6 correction chain
on `PitchThrown` (§11.1 v0.39).
**Cleanup:** delete `ZoneCanvasDemoPage` (`app/lib/src/ui/zone_canvas/zone_canvas_demo_page.dart`) and its
wiring in `main.dart` once the real pitch loop replaces it as the app's home screen — it was DIA-005's
dev-only harness for manually exercising `ZoneCanvas` before real navigation existed.
