# DIA-007 — The pitch loop
**Spec:** §11.1–11.3, §12.5
**Goal:** wire call screen → zone canvas (actual) → outcome confirm (suggested from location, override row)
→ emits PitchThrown with intended+actual; loop returns to call screen; count HUD from DIA-003 projection
(amber uncertainty state); bailout row (two-finger swipe); long-press count → CountCorrection sheet;
top-level undo (VoidEvent).
**Accept:** integration test: scripted 6-pitch AB (incl. one unknown + correction) produces the exact
expected event stream and HUD states. Walk auto-generates forced advances w/ confirm (§11.3).
