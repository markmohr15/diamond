# DIA-002 — Schema codegen pipeline + CI drift check
**Spec:** §21.5(1); tools/codegen/README.md
**Goal:** implement `npm run codegen`: schema/*.json → Dart (app) + TS (server) types. Evaluate quicktype
vs. tiny custom generator; pick and document why. GitHub Actions workflow: run codegen, fail on git diff;
run `flutter test` and fixture suite.
**Accept:** DIA-001's temporary hand types deleted; app compiles on generated types; CI red on manual
edits to generated files; enums round-trip snake_case exactly.
