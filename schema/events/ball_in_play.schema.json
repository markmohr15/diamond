{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://diamond.app/schema/events/ball_in_play.schema.json",
  "title": "BallInPlay",
  "description": "Batted ball incl. fouls with coordinates (spec §4.2, §3.2).",
  "type": "object",
  "properties": {
    "pitchEventId":    { "type": "string" },
    "fair":            { "type": "boolean" },
    "trajectory":      { "enum": ["ground", "line", "fly", "popup", "bunt"] },
    "contactQuality":  { "enum": ["weak", "average", "hard"] },
    "landing":         { "$ref": "../common/field_coord.schema.json", "description": "first contact: where it landed, hit the wall, or met a glove (§15.1)" },
    "endedAt":         { "$ref": "../common/field_coord.schema.json", "description": "where the ball came to rest, when it moved after landing (§15.1). Absent = it stopped where it landed, which is every home run and every ball fielded on the spot. No fielder is implied: a walk-off nobody chases and a ground rule double both end here with nobody near the ball. When a fielder does reach it, her touch carries the location and this is the record from before she was named." },
    "offWall":         { "type": "boolean", "description": "hit the fence on the fly (§15.1, §16.2)" },
    "landingIsCaught": { "type": "boolean" },
    "sacrifice":       { "type": "boolean", "description": "scorer judgment (§13, v0.43): this batted ball was a sacrifice. Required for a sac bunt — no physical record distinguishes bunting to advance a runner from bunting for a hit — and optional for a sac fly, which derives (§13.6) and which this overrides when present. Plate appearance, not an at-bat." }
  },
  "required": ["pitchEventId", "fair", "trajectory", "landing", "landingIsCaught"],
  "additionalProperties": false
}
